#!/usr/bin/python3
class Square:
    """
     Square class handle the `size` field

     Args:
        size (int): Representation of size

     Attributes:
        size (int): Representation of size
     """

    def __init__(self, size):
        self.__size = size
